import 'package:flutter/material.dart';

class MainRedux extends StatelessWidget {
  const MainRedux({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Redux',
      debugShowCheckedModeBanner: false,
    );
  }
}
